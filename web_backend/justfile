setup:
    uv sync
    uvr db.py
    uvr alembic upgrade head
    mkdir static
    mkdir static/dev

db-build:
    uv run db.py
    uv run alembic upgrade head
    uv run python -m seed.seed

seed:
    uv run seed/seed.py
